= render partial: 'sider'
.span9
  - if @store.present?
    .page-header
      %h1=t @store.title

    %p
      %strong description
      %br
      = @store.description

  - else
    .page-header
      %h1 Store Not Found

