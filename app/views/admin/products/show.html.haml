- model_class = Product
.page-header
  %h1=t '.title', :default => model_class.model_name.human.titleize

%p
  %strong= model_class.human_attribute_name(:title) + ':'
  %br
  = @product.title
%p
  %strong= model_class.human_attribute_name(:description) + ':'
  %br
  = @product.description

.form-actions
  = link_to 'ALL', store_products_path(@store), :class => 'btn'
  = link_to 'Edit', edit_store_product_path(@store, @product), :class => 'btn'
  = link_to 'Delete', store_product_path(@store, @product), :method => "delete", :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-danger'
